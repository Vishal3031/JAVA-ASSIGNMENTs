//WAJP to store boolean type data (true) inside the int type variable and print it..

class ACTIVITY6 {
	public static void main(String[] args) {

// Storing boolean (true) inside an int type variable by converting it
        int value = (true) ? 1 : 0;

        // Printing the value of the int variable
        System.out.println("The integer representation of 'true' is: " + value);
    }
}